#include <stdio.h>
int main(){
    float cat1;
    float cat2;
    float hip;
    printf("Digite o valor do primeiro cateto: ");
    scanf("%f", &cat1);
    printf("Digite o valor do segundo cateto: ");
    scanf("%f", &cat2);

    hip = cat1
}